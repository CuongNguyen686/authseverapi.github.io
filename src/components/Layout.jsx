import {NavLink,useLocation} from 'react-router-dom';
import {BarChart3,Box,KeyRound,LayoutDashboard,Menu,Settings,BookOpen,Wallet,X,Bell,Search,ChevronDown,Sparkles,ShieldCheck} from 'lucide-react';
import {useState} from 'react';
import {useStore} from '../state/store';
const items=[['/','Tổng quan',LayoutDashboard],['/keys','API Keys',KeyRound],['/packages','Packages',Box],['/analytics','Analytics',BarChart3],['/docs','Tài liệu',BookOpen],['/pricing','Gói dịch vụ',Wallet],['/settings','Cài đặt',Settings]];
export default function Layout({children}){const [open,setOpen]=useState(false);const [search,setSearch]=useState('');const {displayName,theme,setTheme,notify}=useStore();const loc=useLocation();const title=items.find(x=>x[0]===loc.pathname)?.[1]||'Tổng quan';return <div className="app"><div className="ambient a1"/><div className="ambient a2"/><aside className={`sidebar ${open?'open':''}`}><div className="brand"><span className="logo"><Sparkles size={17}/></span><span>Nguyễn Cường <b>iOS</b><small>ChatGPTDeveloper</small></span><button className="mobile-close" onClick={()=>setOpen(false)}><X size={18}/></button></div><div className="workspace"><ShieldCheck size={15}/> Production workspace <ChevronDown size={13}/></div><nav>{items.map(([to,label,Icon])=><NavLink key={to} to={to} onClick={()=>setOpen(false)} className={({isActive})=>isActive?'active':''}><Icon size={17}/><span>{label}</span>{to==='/keys'&&<em>33</em>}</NavLink>)}</nav><div className="side-bottom"><div className="plan-box"><div><span>Pro Workspace</span><strong>Đang hoạt động</strong></div><button onClick={()=>notify('info','Bạn đang ở gói Pro')}>Quản lý</button></div><div className="version">v2.0.0 • Local API</div></div></aside><div className="main"><header><button className="hamb" onClick={()=>setOpen(true)} aria-label="Mở menu"><Menu/></button><div className="mobile-title"><span className="eyebrow">Nguyễn Cường iOS</span><h1>{title}</h1></div><div className="global-search"><Search size={15}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Tìm nhanh..." aria-label="Tìm nhanh"/>{search&&<button onClick={()=>setSearch('')} aria-label="Xóa tìm kiếm"><X size={13}/></button>}</div><div className="header-actions"><button className="header-icon" onClick={()=>notify('info','Không có thông báo mới')} aria-label="Thông báo"><Bell size={17}/><i/></button><button className="profile" onClick={()=>notify('info','Mở Cài đặt để chỉnh hồ sơ')}><span className="avatar">N</span><span><b>{displayName}</b><small>Pro</small></span><ChevronDown size={14}/></button><button className="theme-chip" onClick={()=>setTheme(theme==='dark'?'light':'dark')} aria-label="Đổi giao diện">{theme==='dark'?'☼':'☾'}</button></div></header><main>{children}</main></div></div>}
