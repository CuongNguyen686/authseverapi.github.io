import {useState} from 'react';import {Copy,Check,Code2,BookOpen,ShieldCheck} from 'lucide-react';import {useStore} from '../state/store';
const tabs=[['overview','Tổng quan'],['api','API Reference'],['sdk','SDK'],['examples','Ví dụ']];
export default function Docs(){const [tab,setTab]=useState('overview');const [copied,setCopied]=useState(false);const {notify}=useStore();const code=`curl -X POST https://api.example.com/v1/validate \\\n  -H "Content-Type: application/json" \\\n  -d '{"package":"YOUR_PACKAGE_TOKEN","key":"YOUR_API_KEY"}'`;const copy=()=>{navigator.clipboard?.writeText(code);setCopied(true);notify('success','Đã copy code mẫu');setTimeout(()=>setCopied(false),1600)};return <div className="page"><div className="page-title"><div><span className="badge">DEVELOPER CENTER</span><h2>Tài liệu tích hợp</h2><p className="muted">Tài liệu frontend độc lập cho workspace Nguyễn Cường iOS.</p></div><div className="doc-version">API v1.0</div></div><div className="docs-layout"><aside className="docs-nav">{tabs.map(([id,label])=><button className={tab===id?'active':''} key={id} onClick={()=>setTab(id)}><BookOpen size={15}/>{label}</button>)}<div className="docs-tip"><ShieldCheck size={17}/><strong>Production ready</strong><small>Service layer đã tách riêng, có thể đổi sang backend thật.</small></div></aside><section className="card prose"><div className="doc-heading"><Code2/><div><h3>{tabs.find(x=>x[0]===tab)?.[1]}</h3><p className="muted">Tích hợp nhanh với API Key Manager.</p></div></div>{tab==='overview'&&<><h3>Bắt đầu nhanh</h3><ol><li>Tạo một Package trong tab Packages.</li><li>Tạo API Key và chọn ngày hết hạn.</li><li>Đưa package token vào adapter/backend của ứng dụng.</li><li>Gọi endpoint validate để kiểm tra key và device.</li></ol><div className="info-box">Mock mode hiện tại dùng localStorage để frontend chạy độc lập trên GitHub Pages.</div></>}{tab==='api'&&<><h3>POST /v1/validate</h3><pre><code>{`{\n  "package": "pk_live_xxx",\n  "key": "NC-123456-7890",\n  "device": "iPhone"\n}`}</code></pre><h3>Response</h3><pre><code>{`{\n  "success": true,\n  "status": "active",\n  "expires": "2026-12-31"\n}`}</code></pre></>}{tab==='sdk'&&<><h3>Service adapter</h3><p>UI không gọi fetch trực tiếp. Toàn bộ request đi qua <code>src/services/api.js</code>, giúp chuyển từ Mock sang REST API mà không đổi component.</p><pre><code>{`import { api } from './services/api';\nconst result = await api.listKeys();`}</code></pre></>}{tab==='examples'&&<><h3>cURL</h3><div className="code-block"><button onClick={copy} className="copy-code">{copied?<Check size={14}/>:<Copy size={14}/>} {copied?'Đã copy':'Copy'}</button><pre><code>{code}</code></pre></div><h3>JavaScript</h3><pre><code>{`const result = await api.validate({\n  package: token,\n  key: apiKey\n});`}</code></pre></>}</section></div></div>}
